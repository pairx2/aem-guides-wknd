$(window).on('load', function () {
    setTimeout(() => {
        $('.modal-dialog-centered .a-container--bg-red').parents('.generic-modal__content').addClass('modal_pedialyte_redBg_popup');
    }, 0);
});
