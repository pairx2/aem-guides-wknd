import * as React from "react";
import LoginForm from "./user-login-dialog/loginForm";
import "../../js/social-login";

const CheckoutLogin = (props={}) => <LoginForm {...props} />
export default CheckoutLogin;
