<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ====================================================================== -->
    <!-- P A R E N T  P R O J E C T  D E S C R I P T I O N                      -->
    <!-- ====================================================================== -->
    <parent>
        <groupId>com.abbott.aem.platform.global</groupId>
        <artifactId>aem-abbott-platform-global.reactor</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <!-- ====================================================================== -->
    <!-- P R O J E C T  D E S C R I P T I O N                                   -->
    <!-- ====================================================================== -->
    <artifactId>aem-abbott-platform-global.ui.apps.structure</artifactId>
    <packaging>content-package</packaging>
    <name>Abbott Platform Global - Repository Structure Package</name>
    <description>
        Empty package that defines the structure of the Adobe Experience Manager repository the Code packages in this project deploy into.
        Any roots in the Code packages of this project should have their parent enumerated in the Filters list below.
    </description>
    
    <properties>
    	<sonar.skip>true</sonar.skip>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.jackrabbit</groupId>
                <artifactId>filevault-package-maven-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <properties>
                        <cloudManagerTarget>none</cloudManagerTarget>
                    </properties>
                    <filters>

                        <!-- /apps root -->
                        <filter><root>/apps</root></filter>

                        <!-- Common overlay roots -->
                        <filter><root>/apps/sling</root></filter>
                        <filter><root>/apps/cq</root></filter>
                        <filter><root>/apps/dam</root></filter>
                        <filter><root>/apps/wcm</root></filter>
                        <filter><root>/apps/msm</root></filter>

                        <!-- Immutable context-aware configurations -->
                        <filter><root>/apps/settings</root></filter>

						<!-- /apps/adc/globals root -->
                        <filter><root>/apps/adc</root></filter>
                        <!-- /apps/cv/globals root -->
                        <filter><root>/apps/cv</root></filter>
                        <filter><root>/apps/epd</root></filter>
						<filter><root>/apps/an</root></filter>
						<filter><root>/apps/cv</root></filter>
						<filter><root>/apps/add</root></filter>
						<filter><root>/apps/corp</root></filter>						
						<filter><root>/apps/bts</root></filter>
						<filter><root>/apps/ardx</root></filter>
						<filter><root>/apps/av</root></filter>                        
						<filter><root>/apps/nm</root></filter>

                    </filters>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
