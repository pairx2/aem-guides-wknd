{
  "externalizer.domains":[
    "local $[env:externalizerDomainLocal;default=http://localhost:4502]",
    "author $[env:externalizerDomainAuthor;default=http://localhost:4502]",
    "publish $[env:externalizerDomainPublish;default=http://localhost:4503]",
{{#each sites}}
    "{{id}} {{#if f7}}https://f7aya7rugada.{{name}}{{else}}{{#if www}}https://$[env:AB_ENVIRONMENT_PREFIX;default=www].{{name}}{{else}}$[env:AB_EXTERNALIZER_PREVIEW_PREFIX;default=https://]{{name}}{{/if}}{{/if}}"{{#unless @last}},{{/unless}}
{{/each}}
  ]
}
